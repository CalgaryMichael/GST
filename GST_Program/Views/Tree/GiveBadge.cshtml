@model GST_Program.Domain.Models.BadgeHistory
@{
    ViewBag.Title = "Give Badge";
}

<h2>Give Badge</h2>

@using (Html.BeginForm()) {
        <div class="form-group">
            <table class="table">
                <tr>
                    <td>Badge</td>
                    <td>
                        @{  var sliBadges = new List<SelectListItem>();
                            foreach (var b in ViewBag.Badges) {
                                string id = Convert.ToString(b.Badge_ID);
                                var item = new SelectListItem { Text = b.Badge_Name, Value = id };
                                sliBadges.Add(item);
                            }
                        }
                        @Html.DropDownList("Badge_ID", sliBadges, "", new { @class = "form-control" })
                    </td>
                </tr>
                <tr>
                    <td>Who Are You?: </td>
                    <td>
                        @{  var sliGiver = new List<SelectListItem>();
                            foreach (var p in ViewBag.People) {
                                string id = Convert.ToString(p.Person_ID);
                                var item = new SelectListItem { Text = p.Person_Name, Value = id };
                                sliGiver.Add(item);
                            }
                        }
                        @Html.DropDownList("Giver_ID", sliGiver, "", new { @class = "form-control" })
                    </td>
                </tr>
                <tr>
                    <td>Send it to: </td>
                    <td>
                        @{  var sliReceiver = new List<SelectListItem>();
                            foreach (var p in ViewBag.People) {
                                string id = Convert.ToString(p.Person_ID);
                                var item = new SelectListItem { Text = p.Person_Name, Value = id };
                                sliReceiver.Add(item);
                            }
                        }
                        @Html.DropDownList("Student_ID", sliReceiver, "", new { @class = "form-control" })
                    </td>
                </tr>
                <tr>
                    <td>
                        Comment: 
                    </td>
                    <td>
                        @Html.TextArea("Comment", "", new { @class = "form-control" })
                    </td>
                </tr>
                <tr>
                    <td></td>
                    <td>
                        <input class="btn btn-default" type="submit" value="Save" />
                        <input class="btn btn-default" type="button" value="Cancel" onclick="window.location='/Admin/BadgeBank/';" />
                    </td>
                </tr>
            </table>
        </div>     
}